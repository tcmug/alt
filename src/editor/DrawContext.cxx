
#include "DrawContext.h"

void DrawContext::print() {
	//const char *str, size_t columns) {
	// size_t i, col, a = 0;
	// for (col=i=0; s[i]; i++) {
	//     if ((unsigned char)str[i] < 0x80 || (unsigned char)str[i] > 0xBF) {
	//         fl_draw(&s[a], i-a, ctx._x, ctx._y);
	//         _x += fl_width(&str[a], i-a);
	//         a = i;
	//         col++;
	//         if (col == columns) {
	//             return x;
	//         }
	//     }
	// }
	// if (_render) {
	//     fl_draw(str i, ctx._x, ctx._y);
	// }
}
/*
void DrawContext::print(const char *str, size_t characters) {
	size_t i, col, a = 0;
	for (col=i=0; s[i]; i++) {
		if ((unsigned char)s[i] < 0x80 || (unsigned char)s[i] > 0xBF) {
			fl_draw(&s[a], i-a, ctx._x, ctx._y);
			ctx._x += fl_width(&s[a], i-a);
			a = i;
			col++;
		}
	}
}
*/
